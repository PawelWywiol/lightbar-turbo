{
  "private": true,
  "scripts": {
    "start": "turbo run start",
    "build": "turbo run build",
    "build:app:esp": "turbo run build --filter esp",
    "dev": "turbo run dev",
    "dev:app:esp": "turbo run dev --filter esp",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.10.3",
    "eslint": "^8.55.0",
    "eslint-config-custom": "*",
    "prettier": "^3.1.1",
    "turbo": "^1.11.2"
  },
  "name": "lightbar-turbo",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "volta": {
    "node": "18.17.1",
    "npm": "10.1.0"
  }
}
